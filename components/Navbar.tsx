"use client";

export default function Navbar() {
  return (
    <nav className="fixed top-0 left-0 w-full bg-black bg-opacity-60 backdrop-blur-md z-50">
      <div className="max-w-4xl mx-auto flex justify-center items-center px-6 py-4">
        {/* Navbar intentionally empty for minimalism */}
      </div>
    </nav>
  );
}
